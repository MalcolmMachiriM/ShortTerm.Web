@model ShortTerm.Web.Models.SystemVariables.HabitsAndInterestsVM

@{
    ViewData["Title"] = "Create Habits & Interests";
}

<div class="col-lg-12 col-12">
    <!-- Basic Forms -->
    <div class="box">
        <div class="box-header with-border">
            <h4 class="box-title">Create Habits & Interests</h4>
        </div>
        <form asp-action="Create">
            <div class="box-body">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="HabitOrInterest" class="control-label"></label>
                    <input asp-for="HabitOrInterest" class="form-control" />
                    <span asp-validation-for="HabitOrInterest" class="text-danger"></span>
                </div>            
                <div class="form-group">
                    <a asp-action="Index" class="btn btn-dark">Back to List</a>
                    <input type="submit" value="Create" class="btn btn-primary" />
                </div>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
